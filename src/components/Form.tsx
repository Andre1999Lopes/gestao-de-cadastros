import styled from 'styled-components';

const Form = styled.form`
	min-height: 4em;
	padding: 1em 0;
	display: flex;
	flex-wrap: wrap;
	justify-content: space-around;
	align-items: center;
`;
export default Form;
